# create new directory
mkdir repos

# switch into
cd repos

# clone the github project
git clone https://github.com/chriskish19/lol.blocks.git

# switch into cmake directory
cd lol.blocks\cmake

# make a build folder
mkdir build

# switch into
cd build

# generate build files
cmake ..

